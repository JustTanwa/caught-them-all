import { createTheme } from '@mui/material';

export const theme = createTheme({
	palette: {
		primary: {
			main: '#f00',
			light: '#ff9a9a',
		},
		secondary: {
			main: '#ffffff',
		},
		third: {
			main: '#000000',
		},
	},
});
